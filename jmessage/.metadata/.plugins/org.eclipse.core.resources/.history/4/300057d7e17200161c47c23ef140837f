package msgclient;

public class TestEncryption {
	static boolean testEncryption(MessageEncryptor encryptor, String message, MsgKeyPair recipientKey) {
		
		// Encrypt the message
		String encryptedMsg = encryptor.encryptMessage(message, recipientKey);
		if (encryptedMsg = null) {
			System.out.println("Failed to encrypt message.");
			return false;
		}
		
		// Decrypt the resulting ciphertext
		String decryptedMsg = encryptor.decryptMessage(encryptedMsg, encryptor.mSenderID, MsgKeyPair senderKey) {


		
	}
}
